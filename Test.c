#include <stdio.h>
#include <stdlib.h>
#include "SDB.c"
int main()
{
    uint32 id = 126;
    // printf("%d", SDB_IsFull());
    SDB_AddEntry();
    // SDB_DeleteEntry(id);
    // SDB_ReadEntry(123);
    // Bool SDB = SDB_IsIdExist(126);
    // printf("\n%d", SDB);
}